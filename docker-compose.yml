version: "3.8"

services:
  app:
    container_name: echo-html
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT=8080
    ports:
      - "8080:8080"
    restart: unless-stopped


